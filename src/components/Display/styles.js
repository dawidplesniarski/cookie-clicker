import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    container: {
        alignItems: 'center',
        width: '100%',
        marginBottom: 60,
    },
    clicksLabel: {
        fontSize: 48,
        fontWeight: 'bold',
        color: '#FFF',
    },
    levelLabel: {
        fontSize: 30,
        fontWeight: '600',
        color: '#FFF',
    },
});

export default styles;
